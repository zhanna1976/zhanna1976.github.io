import { PixelPipePipe } from './pixel-pipe.pipe';

describe('PixelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PixelPipePipe();
    expect(pipe).toBeTruthy();
  });
});
